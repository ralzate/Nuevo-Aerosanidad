<style>
.ids{
	color:gray;
}
</style>
<tr class="x" id=<%= "ciudad_#{ciudad.id}" %>
	>
	<td>
		<%= link_to ciudad.id, edit_ciudad_path(ciudad), :remote =>true, :class => "ids" %></td>
	<td>
		<%= ciudad.nombre.capitalize %></td>
	<td>
		<%= ciudad.pais_codigo.capitalize %></td>

	<td>

		<style type="text/css" media="screen">
.icono {
    font-size: 16px;
    color: #848484;
}
.ver_notas{
color: #666;
}
</style>

		<%= link_to ciudad, remote: true, class: "btn btn-link btn-xs", title: "Ver Ciudad" do %> <i class="glyphicon glyphicon-eye-open icono"></i>
		<% end %>

		<%= link_to edit_ciudad_path(ciudad), remote: true, class: "btn btn-link btn-xs", title: "Editar Ciudad" do %> <i class="glyphicon glyphicon-pencil icono"></i>
		<% end %></td>
</tr>